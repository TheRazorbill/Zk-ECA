# @repo/crypto-core

Pacote compartilhado com utilitarios de prova para o fluxo ZK-ECA.

## API atual

- `createIssuerChallenge()`
- `createAgeProofPayload({ challenge, isOverMinimumAge })`
- `verifyAgeProof({ proof, challenge, minimumAge })`

Esta implementacao e um placeholder para a futura logica criptografica real de ZKP.
